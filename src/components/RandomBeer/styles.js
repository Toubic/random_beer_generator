import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    beerLogo: {
        height: "40vmin",
        pointerEvents: "none",
    },
    typography: {
        color: "black",
    },
}));